import React from "react"
import ReactDOM from "react-dom"

import App from "./App"


ReactDOM.render(<App />, document.getElementById("root"))

//do I need to build a boilerplate HTML for this in order to add styling to HTMl and Body?